import React from 'react';
import Layout from '../../components/Layout';

const index = () => {
    return (
        <Layout>
            Terms and conditions is development now !!
        </Layout>
    );
};

export default index;